<UserControl x:Class="SA.VisualizationSystem.View.GeneralView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:general="clr-namespace:SA.VisualizationSystem.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="500"
             d:DataContext="{d:DesignInstance general:GeneralVm}">
    <Grid>
        
        <ListView FontSize="20" ItemsSource="{Binding ListCollection}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="150" Header="Имя параметра" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Width="100" Header="Значение" DisplayMemberBinding="{Binding Surname}"/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
